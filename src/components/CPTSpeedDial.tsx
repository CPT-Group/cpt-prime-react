import { SpeedDial, SpeedDialProps } from 'primereact/speeddial';

export type CPTSpeedDialProps = SpeedDialProps;

export const CPTSpeedDial = (props: CPTSpeedDialProps) => {
  return <SpeedDial {...props} />;
};

