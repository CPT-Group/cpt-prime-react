import { BreadCrumb, BreadCrumbProps } from 'primereact/breadcrumb';

export type CPTBreadcrumbProps = BreadCrumbProps;

export const CPTBreadcrumb = (props: CPTBreadcrumbProps) => {
  return <BreadCrumb {...props} />;
};

