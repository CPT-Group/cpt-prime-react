import { MegaMenu, MegaMenuProps } from 'primereact/megamenu';

export type CPTMegaMenuProps = MegaMenuProps;

export const CPTMegaMenu = (props: CPTMegaMenuProps) => {
  return <MegaMenu {...props} />;
};

