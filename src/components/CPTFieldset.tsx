import { Fieldset, FieldsetProps } from 'primereact/fieldset';

export type CPTFieldsetProps = FieldsetProps;

export const CPTFieldset = (props: CPTFieldsetProps) => {
  return <Fieldset {...props} />;
};

