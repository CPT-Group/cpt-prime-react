import { TriStateCheckbox, TriStateCheckboxProps } from 'primereact/tristatecheckbox';

export type CPTTriStateCheckboxProps = TriStateCheckboxProps;

export const CPTTriStateCheckbox = (props: CPTTriStateCheckboxProps) => {
  return <TriStateCheckbox {...props} />;
};

