import { Splitter, SplitterProps } from 'primereact/splitter';

export type CPTSplitterProps = SplitterProps;

export const CPTSplitter = (props: CPTSplitterProps) => {
  return <Splitter {...props} />;
};

