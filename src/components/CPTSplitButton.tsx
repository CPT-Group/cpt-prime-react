import { SplitButton, SplitButtonProps } from 'primereact/splitbutton';

export type CPTSplitButtonProps = SplitButtonProps;

export const CPTSplitButton = (props: CPTSplitButtonProps) => {
  return <SplitButton {...props} />;
};

