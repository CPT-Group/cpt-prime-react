import { InplaceContent, InplaceContentProps } from 'primereact/inplace';

export type CPTInplaceContentProps = InplaceContentProps;

export const CPTInplaceContent = (props: CPTInplaceContentProps) => {
  return <InplaceContent {...props} />;
};

