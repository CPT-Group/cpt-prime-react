import { ButtonGroup, ButtonGroupProps } from 'primereact/buttongroup';

export type CPTButtonGroupProps = ButtonGroupProps;

export const CPTButtonGroup = (props: CPTButtonGroupProps) => {
  return <ButtonGroup {...props} />;
};

