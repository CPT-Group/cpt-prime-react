import { RadioButton, RadioButtonProps } from 'primereact/radiobutton';

export type CPTRadioButtonProps = RadioButtonProps;

export const CPTRadioButton = (props: CPTRadioButtonProps) => {
  return <RadioButton {...props} />;
};

