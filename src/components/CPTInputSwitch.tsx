import { InputSwitch, InputSwitchProps } from 'primereact/inputswitch';

export type CPTInputSwitchProps = InputSwitchProps;

export const CPTInputSwitch = (props: CPTInputSwitchProps) => {
  return <InputSwitch {...props} />;
};

