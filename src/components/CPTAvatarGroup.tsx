import { AvatarGroup, AvatarGroupProps } from 'primereact/avatargroup';

export type CPTAvatarGroupProps = AvatarGroupProps;

export const CPTAvatarGroup = (props: CPTAvatarGroupProps) => {
  return <AvatarGroup {...props} />;
};

