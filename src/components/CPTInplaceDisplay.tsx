import { InplaceDisplay, InplaceDisplayProps } from 'primereact/inplace';

export type CPTInplaceDisplayProps = InplaceDisplayProps;

export const CPTInplaceDisplay = (props: CPTInplaceDisplayProps) => {
  return <InplaceDisplay {...props} />;
};

